interface:
  display_name: "Fix"
  short_description: "Run an explicitly Guarded, evidence-bound repair"
  default_prompt: "Use $fix to stage one compact Guarded repair contract, show its easy-language explanation with the original available on request, wait for later-turn approval, then repair it with evidence-bound capabilities."
policy:
  allow_implicit_invocation: false
