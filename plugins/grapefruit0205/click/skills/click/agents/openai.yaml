interface:
  display_name: "Click"
  short_description: "Keep revision-aware evidence; guard risky work when needed"
  default_prompt: "Use $click in Evidence mode for approval-free revision-aware receipts, or choose Guarded to show one four-section contract, wait for later-turn approval, and execute inside that boundary."
policy:
  allow_implicit_invocation: true
